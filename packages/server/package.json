{
  "name": "@ffwf/tau-code-server",
  "version": "0.3.0",
  "description": "Connection server: owns a tau process and serves it to browser clients over WebSocket.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "tau-code-server": "./dist/cli.js"
  },
  "//files": "dist-web is the built web client, copied in by `build`. It ships here rather than being read out of the sibling workspace, because the sibling only exists in a checkout.",
  "files": ["dist", "dist-web"],
  "scripts": {
    "build": "tsc --build && node copy-web.mjs",
    "dev": "node --watch dist/cli.js"
  },
  "dependencies": {
    "@ffwf/tau-code-protocol": "0.3.0",
    "@ffwf/tau-code-runner": "0.3.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.12"
  }
}
